Nodes:
    A₁ = Φ(A₀, A₂)
    B₁ = Φ(B₀, B₂)
    C₁ = Φ(C₀, C₂)
    D₁ = Φ(D₀, D₂)
    !1₂ = (B₁[i] + 10)
    A₂ = Update(A₁, i, !1₂)
    !2₂ = (A₂[i] * D₁[(i - 1)])
    B₂ = Update(B₁, i, !2₂)
    !3₂ = (A₂[i] * D₁[(i - 1)])
    C₂ = Update(C₁, i, !3₂)
    !4₂ = (B₂[i] * C₂[i])
    D₂ = Update(D₁, i, !4₂)
    !5₁ = (A₁, B₁, C₁, D₁)
Forward edges:
    A₁ = Φ(A₀, A₂)  →  A₂ = Update(A₁, i, !1₂)
    A₁ = Φ(A₀, A₂)  →  !5₁ = (A₁, B₁, C₁, D₁)
    B₁ = Φ(B₀, B₂)  →  !1₂ = (B₁[i] + 10)
    B₁ = Φ(B₀, B₂)  →  B₂ = Update(B₁, i, !2₂)
    B₁ = Φ(B₀, B₂)  →  !5₁ = (A₁, B₁, C₁, D₁)
    C₁ = Φ(C₀, C₂)  →  C₂ = Update(C₁, i, !3₂)
    C₁ = Φ(C₀, C₂)  →  !5₁ = (A₁, B₁, C₁, D₁)
    D₁ = Φ(D₀, D₂)  →  !2₂ = (A₂[i] * D₁[(i - 1)])
    D₁ = Φ(D₀, D₂)  →  !3₂ = (A₂[i] * D₁[(i - 1)])
    D₁ = Φ(D₀, D₂)  →  D₂ = Update(D₁, i, !4₂)
    D₁ = Φ(D₀, D₂)  →  !5₁ = (A₁, B₁, C₁, D₁)
    !1₂ = (B₁[i] + 10)  →  A₂ = Update(A₁, i, !1₂)
    A₂ = Update(A₁, i, !1₂)  →  !2₂ = (A₂[i] * D₁[(i - 1)])
    A₂ = Update(A₁, i, !1₂)  →  !3₂ = (A₂[i] * D₁[(i - 1)])
    !2₂ = (A₂[i] * D₁[(i - 1)])  →  B₂ = Update(B₁, i, !2₂)
    B₂ = Update(B₁, i, !2₂)  →  !4₂ = (B₂[i] * C₂[i])
    !3₂ = (A₂[i] * D₁[(i - 1)])  →  C₂ = Update(C₁, i, !3₂)
    C₂ = Update(C₁, i, !3₂)  →  !4₂ = (B₂[i] * C₂[i])
    !4₂ = (B₂[i] * C₂[i])  →  D₂ = Update(D₁, i, !4₂)
Back edges:
    A₂ = Update(A₁, i, !1₂)  →  A₁ = Φ(A₀, A₂)
    B₂ = Update(B₁, i, !2₂)  →  B₁ = Φ(B₀, B₂)
    C₂ = Update(C₁, i, !3₂)  →  C₁ = Φ(C₀, C₂)
    D₂ = Update(D₁, i, !4₂)  →  D₁ = Φ(D₀, D₂)
