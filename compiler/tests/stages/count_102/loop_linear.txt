def count_102(Seq: shared[list[int]], N: plaintext[int], Syms: shared[list[int]]):
    s0₁ = False
    c₁ = 0
    for i in range(0, N₀):
        s0₂ = Φ(s0₁, s0₃)
        c₂ = Φ(c₁, c₄)
        !1₂ = (Seq₀[i] == Syms₀[2])
        !2₂ = (s0₂ and !1₂)
        c₃ = (c₂ + 1)
        c₄ = MUX(!2₂, c₃, c₂)
        !3₂ = (Seq₀[i] == Syms₀[1])
        !5₂ = (Seq₀[i] == Syms₀[0])
        !6₂ = (s0₂ and !5₂)
        s0₃ = (!3₂ or !6₂)
    return c₂
