Nodes:
    max_dist₁ = 0
    current_dist₁ = 0
    max_dist₂ = Φ(max_dist₁, max_dist₄)
    current_dist₂ = Φ(current_dist₁, current_dist₅)
    !1₂ = (Seq₀[i] == Sym₀)
    !2₂ = not !1₂
    current_dist₄ = 0
    current_dist₃ = (current_dist₂ + 1)
    current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)
    !3₂ = (current_dist₅ > max_dist₂)
    max_dist₃ = current_dist₅
    max_dist₄ = MUX(!3₂, max_dist₃, max_dist₂)
Forward edges:
    max_dist₁ = 0  →  max_dist₂ = Φ(max_dist₁, max_dist₄)
    current_dist₁ = 0  →  current_dist₂ = Φ(current_dist₁, current_dist₅)
    max_dist₂ = Φ(max_dist₁, max_dist₄)  →  !3₂ = (current_dist₅ > max_dist₂)
    max_dist₂ = Φ(max_dist₁, max_dist₄)  →  max_dist₄ = MUX(!3₂, max_dist₃, max_dist₂)
    current_dist₂ = Φ(current_dist₁, current_dist₅)  →  current_dist₃ = (current_dist₂ + 1)
    !1₂ = (Seq₀[i] == Sym₀)  →  !2₂ = not !1₂
    !2₂ = not !1₂  →  current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)
    current_dist₄ = 0  →  current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)
    current_dist₃ = (current_dist₂ + 1)  →  current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)
    current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)  →  !3₂ = (current_dist₅ > max_dist₂)
    current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)  →  max_dist₃ = current_dist₅
    !3₂ = (current_dist₅ > max_dist₂)  →  max_dist₄ = MUX(!3₂, max_dist₃, max_dist₂)
    max_dist₃ = current_dist₅  →  max_dist₄ = MUX(!3₂, max_dist₃, max_dist₂)
Back edges:
    current_dist₅ = MUX(!2₂, current_dist₃, current_dist₄)  →  current_dist₂ = Φ(current_dist₁, current_dist₅)
    max_dist₄ = MUX(!3₂, max_dist₃, max_dist₂)  →  max_dist₂ = Φ(max_dist₁, max_dist₄)
