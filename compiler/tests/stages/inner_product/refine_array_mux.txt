def ip(A: shared[list[int]], B: shared[list[int]], N: plaintext[int]):
    sum₁ = 0
    for i in range(0, N₀):
        sum₂ = Φ(sum₁, sum₃)
        temp₂ = (A₀[i] * B₀[i])
        sum₃ = (sum₂ + temp₂)
    return sum₂
